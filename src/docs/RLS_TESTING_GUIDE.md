# RLS Policy Testing Guide

Use the following SQL queries in the Supabase SQL Editor to verify that policies are working as expected.

## 1. Verify Public Access
Check if a public user (anon) can see published content but not draft content.