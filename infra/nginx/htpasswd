# This is a sample htpasswd file for basic authentication.
# To generate your own, you can use an online tool or the `htpasswd` command-line utility.
# For example, on a system with apache2-utils installed:
# htpasswd -c htpasswd your_username

# The format is: username:hashed_password
# The example below is for user "dbadmin" with password "supersecretpassword"
dbadmin:$apr1$j8bczs2g$qB88HLLQ07Uyr5yUTohzR.